@page
@model VisaFusion.Web.Areas.Reporting.Pages.TodayTransactionModel
@using VisaFusion.Web.Components
@{
    ViewData["Title"] = "Today Transaction";
    ViewData["UseSidebar"] = true;

    var dataTable = new DataTableModel
    {
        Id = "today-transaction-table",
        AriaLabel = "Today transaction report",
        Columns = new List<DataTableColumn>
        {
            new() { Key = "Refno", Title = "Ref", Width = "80px" },
            new() { Key = "VisafeeTotal", Title = "Visa fee", Width = "120px", CellClass = "text-end",
                Formatter = (v) => v is decimal d ? d.ToString("0.00") : (v?.ToString() ?? "—") },
            new() { Key = "HotelTotal", Title = "Hotel", Width = "120px", CellClass = "text-end",
                Formatter = (v) => v is int i ? i.ToString() : (v?.ToString() ?? "—") },
            new() { Key = "CabTotal", Title = "Cab", Width = "120px", CellClass = "text-end",
                Formatter = (v) => v is int i ? i.ToString() : (v?.ToString() ?? "—") }
        },
        Rows = Model.Rows.Select(r => new Dictionary<string, object>
        {
            ["Refno"] = r.Refno ?? 0,
            ["VisafeeTotal"] = r.VisafeeTotal ?? 0m,
            ["HotelTotal"] = r.HotelTotal ?? 0,
            ["CabTotal"] = r.CabTotal ?? 0
        }).ToList(),
        ShowPagination = false,
        EmptyMessage = "No transaction entries.",
        Responsive = true
    };
}

<div class="card shadow-sm mb-4">
    <div class="card-body">
        <form method="get" class="row g-3 align-items-end" novalidate>
            <div class="col-auto">
                <label for="dateFrom" class="form-label">From</label>
                <input type="date" id="dateFrom" name="dateFrom" class="form-control"
                       value="@(Model.Params?.From.ToString("yyyy-MM-dd"))" />
            </div>
            <div class="col-auto">
                <label for="dateTo" class="form-label">To</label>
                <input type="date" id="dateTo" name="dateTo" class="form-control"
                       value="@(Model.Params?.To.HasValue == true ? Model.Params!.To.Value.AddDays(-1).ToString("yyyy-MM-dd") : "")" />
            </div>
            <div class="col-auto">
                <label for="agentId" class="form-label">Agent ID (optional)</label>
                <input type="number" id="agentId" name="agentId" class="form-control"
                       value="@(Model.Params?.AgentId?.ToString())" />
            </div>
            <div class="col-auto">
                <button type="submit" class="btn btn-primary">Filter</button>
            </div>
        </form>
    </div>
</div>

<p class="text-body-secondary">One row per reference number entered in the range (defaults to today) with per-refno fee totals.</p>

@if (!string.IsNullOrWhiteSpace(Model.ParamError))
{
    <div class="alert alert-danger" role="alert">@Model.ParamError</div>
}

<partial name="~/Components/_DataTable.cshtml" model="dataTable" />