@page
@model VisaFusion.Web.Areas.Reporting.Pages.TodayCollectionModel
@{
    ViewData["Title"] = "Today Collection";
    ViewData["UseSidebar"] = true;
}

@section SidebarNav {
    <a asp-area="Reporting" asp-page="/Pending">Pending</a>
    <a asp-area="Reporting" asp-page="/TodaySubmission">Today Submission</a>
    <a asp-area="Reporting" asp-page="/TodayCollection" aria-current="page">Today Collection</a>
    <a asp-area="Reporting" asp-page="/TodayTransaction">Today Transaction</a>
    <a asp-area="Reporting" asp-page="/DailyVisaFee">Daily Visa Fee</a>
    <a asp-area="Reporting" asp-page="/DailyBill">Daily Bill</a>
}

<div class="vf-content">
    <h1>Today Collection</h1>
    <p>Collections received in the selected date range (defaults to today).</p>

    <form method="get" class="vf-inline-form">
        <div class="form-group">
            <label for="dateFrom">From</label>
            <input type="date" id="dateFrom" name="dateFrom" class="form-control"
                   value="@(Model.Params?.From.ToString("yyyy-MM-dd"))" />
        </div>
        <div class="form-group">
            <label for="dateTo">To</label>
            <input type="date" id="dateTo" name="dateTo" class="form-control"
                   value="@(Model.Params?.To is { } to ? to.AddDays(-1).ToString("yyyy-MM-dd") : string.Empty)" />
        </div>
        <div class="form-group">
            <label for="agentId">Agent ID (optional)</label>
            <input type="number" id="agentId" name="agentId" class="form-control"
                   value="@(Model.Params?.AgentId?.ToString())" />
        </div>
        <button type="submit" class="vf-btn vf-btn-primary">Filter</button>
    </form>

    @if (!string.IsNullOrWhiteSpace(Model.ParamError))
    {
        <div class="vf-alert vf-alert-danger" role="alert">@Model.ParamError</div>
    }

    @if (Model.Rows.Count == 0)
    {
        <p>No collections in the selected range.</p>
    }
    else
    {
        <table class="vf-table">
            <thead>
                <tr>
                    <th scope="col">Ref</th>
                    <th scope="col">Passenger</th>
                    <th scope="col">Agent</th>
                    <th scope="col">Country</th>
                    <th scope="col">Status</th>
                    <th scope="col">Collected</th>
                    <th scope="col">Submitted</th>
                    <th scope="col">Sent</th>
                    <th scope="col">Remarks</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var row in Model.Rows)
                {
                    <tr>
                        <td>@row.Refno</td>
                        <td>@row.Paxname</td>
                        <td>@row.Agentname</td>
                        <td>@row.Countryname</td>
                        <td>@row.StatusId</td>
                        <td>@(row.Coldate?.ToString("dd-MMM-yyyy") ?? "—")</td>
                        <td>@(row.Subdate?.ToString("dd-MMM-yyyy") ?? "—")</td>
                        <td>@(row.SentDate?.ToString("dd-MMM-yyyy") ?? "—")</td>
                        <td>@(string.IsNullOrWhiteSpace(row.Remarks) ? "—" : row.Remarks)</td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>