namespace VisaFusion.Data.Persistence.Entities;

/// <summary>
/// Maps the legacy `CountryInfo` table (data-model.md §1). Scaffolding entity only.
/// </summary>
public class CountryInfo
{
    public long Id { get; set; }
}