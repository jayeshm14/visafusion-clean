namespace VisaFusion.Data.Persistence.Entities;

/// <summary>
/// Maps the legacy `weeklyoff` table (data-model.md §1). Scaffolding entity only.
/// </summary>
public class WeeklyOff
{
    public long Id { get; set; }
}