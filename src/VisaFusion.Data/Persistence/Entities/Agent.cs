namespace VisaFusion.Data.Persistence.Entities;

/// <summary>
/// Maps the legacy `agents` table (data-model.md §1). Scaffolding entity only.
/// </summary>
public class Agent
{
    public long Id { get; set; }
}