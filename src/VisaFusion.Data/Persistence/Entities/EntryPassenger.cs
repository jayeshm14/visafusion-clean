namespace VisaFusion.Data.Persistence.Entities;

/// <summary>
/// Maps the legacy `entryDetails` table (data-model.md §1). Scaffolding entity only.
/// </summary>
public class EntryPassenger
{
    public long Id { get; set; }
}