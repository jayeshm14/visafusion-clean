# Migration & Scripting Requirements Quality Checklist: Core Entry Workflow (SPEC-0006)

**Purpose**: Unit tests for the quality, clarity, and completeness of the SPEC-0006 migration/scripting requirements — the owner-supplied T-SQL scripts (01-08), cleansing rules, pre-cutover verification, and cutover behavior. Validates the requirements writing, not the implementation.
**Created**: 2026-08-16
**Feature**: [spec.md](../spec.md) | **Plan**: [plan.md](../plan.md) | **Data model**: [data-model.md](../data-model.md) | **Deviation log**: [deviation-log.md](../deviation-log.md) | **Tasks**: [tasks.md](../tasks.md)

**Note**: This checklist is generated by the `/speckit.checklist` command based on feature context and requirements.

## Requirement Completeness

- [ ] CHK001 Are requirements defined for ALL owner-supplied scripts (01-08) — creation, execution order, and verbatim recreation (GR-0001), with the file inventory listed? [Completeness, Spec §16/GR-0001]
- [ ] CHK002 Are requirements specified for NULL-agent handling (FR-005c) — the read/update behavior for the 6,517 orphaned Mainentry rows (data-model.md §1)? [Completeness, Spec §FR-005c/§16]
- [ ] CHK003 Are requirements specified for the 100%-NULL `entrytype` defaulting (FR-005b) — is the cleansing rule (b) stated as a requirement with a defined default value? [Completeness, Spec §FR-005b/§16]
- [ ] CHK004 Are requirements defined for orphaned-agent reconciliation (`usp_Migrate_ReconcileOrphanAgents`) — the target state and its verification? [Completeness, Spec §16]
- [ ] CHK005 Are requirements specified for the statusID=508 duplicate-description cleansing (`usp_Migrate_CleanseStatus508`)? [Completeness, Spec §16/GR-0002]
- [ ] CHK006 Are requirements defined for the pre-cutover column-name verification — does it cover ALL column references in scripts 01-08, not only those carrying `-- TODO: confirm column name` markers? [Completeness, Spec §Assumptions/GR-0001]
- [ ] CHK007 Are requirements specified for the §24 traceability matrix — does it include every FR (including FR-005b/FR-005c) and every AC (AC-001..AC-011)? [Completeness, Spec §24]

## Requirement Clarity

- [ ] CHK008 Is the column-name requirement unambiguous — do the scripts consistently reference `Mainentry.agent` (verified migration lines 642/677-681) with no residual `agentid` references in scripts 03/04/05? [Clarity, Spec §16]
- [ ] CHK009 Is the "required fields per legacy entryDetails schema" (§17) actually enumerated — is the field list specified rather than referenced circularly? [Clarity, Spec §17]
- [ ] CHK010 Is the refno type-conversion requirement unambiguous — is the BIGINT→int conversion (deviation-log entry 2) stated as a requirement with the truncation-risk rationale? [Clarity, Spec §BR-001/deviation-log]
- [ ] CHK011 Is the script execution order (01-08) and its dependency rationale specified — why this order, and what happens if a later script runs first? [Clarity, Spec §16/GR-0001]
- [ ] CHK012 Is the cleansing-rule precedence specified — when multiple cleansing rules apply to the same row (e.g., NULL agent AND NULL entrytype), which rule wins? [Clarity, Spec §16]

## Requirement Consistency

- [ ] CHK013 Do the scripts and the spec reference the same column names consistently — `agent` (not `agentid`) across scripts 03/04/05 and 06/07/08? [Consistency, Spec §16]
- [ ] CHK014 Is the `usp_` prefix convention consistent across ALL scripts (01-08) with no residual `sp_` references? [Consistency, Spec §8/GR-0003]
- [ ] CHK015 Is FR-005b/FR-005c coverage consistent between spec §9 and tasks.md — are both requirements mapped to implementation tasks? [Consistency, Spec §9/tasks.md]
- [ ] CHK016 Is the §24 traceability matrix consistent with §9 — do the matrix rows match the FR list (including FR-005b/FR-005c)? [Consistency, Spec §9/§24]
- [ ] CHK017 Is the service-placement deviation (deviation-log entry 1) consistent with the constitution's ADR requirement — is the architectural decision documented as an ADR-XXXX under /adr (Principle IV)? [Consistency, Constitution Principle IV]

## Acceptance Criteria Quality

- [ ] CHK018 Is the cleansing success criterion measurable — does it define row counts before/after and the expected residual (e.g., 0 orphans after reconcile)? [Measurability, Spec §AC-001]
- [ ] CHK019 Is the NULL-agent acceptance criterion verifiable — does it define the observable behavior (read succeeds, update preserves NULL, or assigns a default)? [Measurability, Spec §FR-005c]
- [ ] CHK020 Is the entrytype-defaulting criterion verifiable — does it define the default value and the verification query? [Measurability, Spec §FR-005b]
- [ ] CHK021 Is the script idempotency criterion measurable — does AC-010/NFR-001 define the re-run verification (CREATE OR ALTER as a safe no-op)? [Measurability, Spec §NFR-001/AC-010]

## Scenario Coverage

- [ ] CHK022 Are requirements defined for the cutover scenario — script execution against live data at the data-volume baseline (Mainentry ≈ 271,724 rows)? [Coverage, Spec §13/§23]
- [ ] CHK023 Are requirements defined for the partial-failure scenario — a script fails mid-run; is the rollback/re-run behavior specified? [Coverage, Gap]
- [ ] CHK024 Are requirements defined for the re-run scenario — scripts re-executed after a failed cutover; is the expected state (partial vs clean) defined? [Coverage, Gap]
- [ ] CHK025 Are requirements defined for the post-cutover validation scenario — how is migrated data verified against legacy (row counts, checksums)? [Coverage, Spec §AC-001]
- [ ] CHK026 Are requirements defined for the environment-dependency scenario — SQL Server availability for cutover (deviation-log entry 3 documents no local SQL Server)? [Coverage, Gap]

## Edge Case Coverage

- [ ] CHK027 Is the orphaned-agent edge case (6,517 rows) addressed in requirements — NULL agent on entry read/update? [Edge Case, Spec §FR-005c/§16]
- [ ] CHK028 Is the 100%-NULL `entrytype` edge case addressed — the defaulting rule (b) as a requirement, not only an assumption? [Edge Case, Spec §16/§Assumptions]
- [ ] CHK029 Is the statusID=508 duplicate-description edge case addressed — the cleansing requirement stated? [Edge Case, Spec §16/GR-0002]
- [ ] CHK030 Is the refno-gap edge case addressed — sequence start above current max with "gaps acceptable" (BR-001) as a migration constraint? [Edge Case, Spec §BR-001/§Assumptions]
- [ ] CHK031 Is the BIGINT-refno overflow edge case addressed — is the truncation-risk rationale (deviation-log entry 2) documented as a requirement? [Edge Case, Spec §BR-001]

## Non-Functional Requirements

- [ ] CHK032 Are the stored-proc performance targets specified (usp_AllocateNextRefno < 50ms, usp_RecordEntryStatusChange < 100ms) with a defined measurement environment? [Clarity, Spec §13]
- [ ] CHK033 Is the idempotency/reversibility requirement (NFR-001, AC-010) applied to the script execution order — is re-running the CREATE OR ALTER scripts a safe no-op specified? [Completeness, Spec §NFR-001/AC-010]
- [ ] CHK034 Is the no-plaintext-password requirement applied to the su-provisioning script — pre-hashed input only, at both layers? [Completeness, Spec §12/§FR-007]

## Dependencies & Assumptions

- [ ] CHK035 Is the dependency on the owner-supplied scripts (01-08, run in order at cutover) documented as a hard prerequisite with the file inventory? [Dependency, Spec §Assumptions/GR-0001]
- [ ] CHK036 Is the dependency on SPEC-0004 (38-entity schema, VisaEntryDbContext mappings) explicit for the migration scripts? [Dependency, Spec §Assumptions/§8]
- [ ] CHK037 Is the "legacy VisaEntry database accessible read-only" assumption validated — is there a requirement to verify read access before cutover? [Assumption, Spec §Assumptions]
- [ ] CHK038 Is the SQL Server availability assumption documented — cutover requires a reachable SQL Server (deviation-log entry 3)? [Assumption, Spec §Assumptions]

## Ambiguities & Conflicts

- [ ] CHK039 Is the agentid-vs-agent conflict resolved in the requirements — does the spec state the verified column name (`agent`) and the correction applied to scripts 03/04/05? [Conflict, Spec §16]
- [ ] CHK040 Is the T004 verification scope unambiguous — does the pre-cutover verification requirement cover ALL column references, not just TODO-marked ones? [Ambiguity, Spec §Assumptions/tasks.md T004]
- [ ] CHK041 Is the FR-005b/FR-005c task-coverage gap resolved — are the requirements mapped to tasks, or is the gap explicitly deferred? [Ambiguity, Spec §9/tasks.md]
- [ ] CHK042 Is the §17 "required fields" reference unambiguous — is the field list specified in the spec rather than deferred? [Ambiguity, Spec §17]
- [ ] CHK043 Is the US1 FR-attribution consistent — does the user story reference the correct FRs (FR-002, BR-005) without FR-001 (SPEC-0004 scope)? [Consistency, Spec §14.1/tasks.md]
- [ ] CHK044 Is the HolidayService dependency in the API requirements justified — is there a stated requirement for bookable-date validation on any Entries endpoint (T027 depends on T020)? [Ambiguity, Spec §15/tasks.md T027]

## Notes

- Check items off as completed: `[x]`
- Add comments or findings inline
- Link to relevant resources or documentation
- Items are numbered sequentially for easy reference
- Items CHK001-CHK044 reference spec.md sections verbatim; cross-references: [data-model.md](../data-model.md), [deviation-log.md](../deviation-log.md), [tasks.md](../tasks.md), [contracts/entries-api.md](../contracts/entries-api.md)